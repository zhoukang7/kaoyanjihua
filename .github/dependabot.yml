# Weekly dependency review for immutable GitHub Actions references.
# Validation-only change: confirms the protected branch recognizes the build check.
version: 2

updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    labels:
      - "dependencies"
      - "security"
